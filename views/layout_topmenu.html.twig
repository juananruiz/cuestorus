<nav class="navbar navbar-fixed-top navbar-inverse">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse"
                    data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="#">Recursos Humanos</a>
        </div>
        <div class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
                <li {% if 'inicio' == active %}class="active"{% endif %}><a href="{{ path('homepage') }}"><i
                                class="fa fa-home"></i>  {{ 'Inicio' }}</a></li>
                <li {% if 'preguntas' == active %}class="active"{% endif %}>
                    <a href="{{ path('preguntas') }}"><i class="fa fa-question"></i> Preguntas</a>
                </li>
                <li {% if 'participantes' == active %}class="active"{% endif %}>
                    <a href="{{ path('participantes') }}"><i class="fa fa-users"></i> Participantes</a>
                </li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                <li {% if 'me' == active %}class="active"{% endif %}><a href="#"><i
                                class="icon-user"></i> Mi perfil</a></li>
                <li><a href="#">{{ 'Logout' }}</a></li>
                <li {% if 'login' == active %}class="active"{% endif %}><a href="#"><i
                                class="icon-user"></i> {{ 'Login' }}</a></li>
            </ul>
        </div>
    </div>
</nav>